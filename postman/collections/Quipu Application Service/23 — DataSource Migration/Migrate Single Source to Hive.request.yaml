$kind: http-request
url: "{{baseUrl}}/migrate-data-source/hive-source/:sourceId"
method: GET
headers:
  Authorization: Bearer {{token}}
  X-Tenant-Id: "{{tenantId}}"
pathVariables:
  sourceId: "1"
order: 4000
