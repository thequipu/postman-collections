$kind: http-request
url: "{{baseUrl}}/migrate-data-source/create-trino-catalog/:sourceId"
method: GET
headers:
  Authorization: Bearer {{token}}
  X-Tenant-Id: "{{tenantId}}"
pathVariables:
  sourceId: "1"
order: 6000
