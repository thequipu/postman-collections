$kind: http-request
url: "{{baseUrl}}/realm"
method: PUT
headers:
  Authorization: Bearer {{token}}
  Content-Type: application/json
body:
  type: json
  content: |-
    {
      "id": 1,
      "name": "production-realm",
      "schemaName": "prod_schema",
      "description": "Updated production fabric"
    }
order: 11000
