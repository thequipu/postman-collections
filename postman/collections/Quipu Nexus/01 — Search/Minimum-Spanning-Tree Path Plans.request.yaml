$kind: http-request
url: "{{baseUrl}}/nexus/mst"
method: POST
headers:
  Authorization: Bearer {{token}}
  Content-Type: application/json
  X-Tenant-ID: "{{tenantId}}"
body:
  type: json
  content: |-
    {
      "fabricId": "production-fabric",
      "query": "customers who ordered from Acme Corp",
      "topK": 15,
      "skipUnstructured": false
    }
order: 5000
