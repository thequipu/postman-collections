$kind: http-request
url: "{{baseUrl}}/s3-upload/getExcelHeader?datasourceName=sales-excel"
method: POST
headers:
  Authorization: Bearer {{token}}
  Content-Type: application/vnd.quipu.file-upload+json;version=1.0.0
queryParams:
  datasourceName: sales-excel
body:
  type: json
  content: |-
    {
      "bucket": "quipu-data",
      "key": "realm-1/sales.xlsx",
      "region": "us-east-1",
      "accessKey": "AKIA...",
      "secret": "secret"
    }
order: 1000
