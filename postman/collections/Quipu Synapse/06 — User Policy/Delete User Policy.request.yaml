$kind: http-request
url: "{{baseUrl}}/users/:userId/policy?namespace={{namespace}}"
method: DELETE
headers:
  Authorization: Bearer {{token}}
queryParams:
  namespace: "{{namespace}}"
pathVariables:
  userId: "{{userId}}"
order: 3000
