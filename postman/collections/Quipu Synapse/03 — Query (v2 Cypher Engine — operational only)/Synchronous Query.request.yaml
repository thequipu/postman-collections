$kind: http-request
url: "{{baseUrl}}/api/v2/cypher/query"
method: POST
headers:
  Authorization: Bearer {{token}}
  Content-Type: application/json
body:
  type: json
  content: |-
    {
      "query": "MATCH (n:Person) RETURN n LIMIT 25",
      "namespace": "{{namespace}}"
    }
order: 1000
