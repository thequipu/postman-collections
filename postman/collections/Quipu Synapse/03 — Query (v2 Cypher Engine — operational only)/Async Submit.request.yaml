$kind: http-request
url: "{{baseUrl}}/api/v2/cypher/async"
method: POST
headers:
  Authorization: Bearer {{token}}
  Content-Type: application/json
body:
  type: json
  content: |-
    {
      "query": "MATCH (n:Person) RETURN n LIMIT 100",
      "namespace": "{{namespace}}"
    }
order: 4000
