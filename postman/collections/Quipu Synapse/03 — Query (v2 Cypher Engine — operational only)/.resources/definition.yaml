$kind: collection
description: The v2 openCypher engine (query/cypher2, ANTLR parse -> logical/physical plan -> streaming runtime). Base path /api/v2/cypher. This folder covers only the 6 operational endpoints with a minimal example — for exhaustive per-construct coverage (MATCH/WHERE/CREATE/MERGE/UNWIND/etc.) import postman/cypher2.postman_collection.json instead, which is purpose-built for this engine and generated by gen_collection.py.
order: 3000
