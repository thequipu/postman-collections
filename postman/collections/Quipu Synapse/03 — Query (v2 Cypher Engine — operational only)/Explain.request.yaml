$kind: http-request
url: "{{baseUrl}}/api/v2/cypher/explain"
method: POST
headers:
  Authorization: Bearer {{token}}
  Content-Type: application/json
body:
  type: json
  content: |-
    {
      "query": "MATCH (n:Person {uri: 'urn:person/1'})-[:KNOWS]->(m) RETURN m",
      "namespace": "{{namespace}}"
    }
order: 2000
