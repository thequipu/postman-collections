$kind: http-request
url: "{{baseUrl}}/namespaces/:name?permanent=false"
method: DELETE
headers:
  Authorization: Bearer {{token}}
queryParams:
  permanent: "false"
pathVariables:
  name: "{{namespace}}"
order: 8000
